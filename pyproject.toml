[project]
name = "medium-rag-assignment"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.8.0",
    "pinecone>=7.0.0",
    "urllib3<2",
]

[tool.vercel]
entrypoint = "api.index:handler"
